const init = async () => {

};

// load and execute and start script after page fully load
document.addEventListener("DOMContentLoaded", async () => {
    
    // if we are in the home page load all games
    await init();
});